'use strict'
const db = require('../util/db')
const redis = require('../util/redis')
const conf = require('../util/conf')
const storage = require('../util/storage')


class UpDown {
  constructor() {
  }

  async up(uid, id, cancel) {

  }

  async down(uid, id, cancel) {
    
  }
}

exports.UpDown = UpDown
